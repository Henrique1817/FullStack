<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Blog — LAB 9</title>
    <link rel="stylesheet" href="/primeiroSemetres/blog-lab.css" />
  </head>
  <body class="blog-page">
    <div class="blog-wrap">
      <header class="blog-header">
        <h1>Blog</h1>
        <p class="blog-lead">Posts armazenados no SQLite (create + read).</p>
        <a class="btn-novo" href="/primeiroSemetres/cadastrar_post.html">Criar novo post</a>
      </header>

      <main class="blog-main">
        <% if (!posts || posts.length === 0) { %>
          <p class="sem-posts">Nenhum post ainda. Clique em “Criar novo post”.</p>
        <% } else { %>
          <ul class="lista-posts">
            <% posts.forEach(function (p) { %>
              <li class="cartao-post">
                <h2><%= p.titulo %></h2>
                <p class="meta">#<%= p.id %> · <%= p.criado_em %></p>
                <p class="resumo"><%= p.resumo %></p>
                <div class="conteudo"><%= p.conteudo %></div>
              </li>
            <% }); %>
          </ul>
        <% } %>
      </main>

      <nav class="blog-nav">
        <a href="/primeiroSemetres/projects.html">Projetos</a>
        <a href="/primeiroSemetres/index.html">Portal</a>
      </nav>
    </div>
  </body>
</html>
